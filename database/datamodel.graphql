type User {
  id: ID! @unique
  name: String!
  email: String! @unique
  password: String!
  # groups: [Group!]!
  # balance
}

type Group {
  id: ID! @unique
  name: String!
  # expenses: [Expense!]!
}
