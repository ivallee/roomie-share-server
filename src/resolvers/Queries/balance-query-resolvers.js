const { validateUser, validateGroupMembership } = require('../../utils');

async function balance(parent, args, ctx, info) {


  return {
    // userId
    // group
    // total paid
    // total owed
  }
}

module.exports = {
  balance
}