
type Query {
  allUsers: [User!]!
  allGroups: [Group!]!
  getUserById(userId: String!): User!
}

type User {
  id: ID!
  name: String!
  email: String!
  groups: [Group!]!
}

type Group {
  id: ID!
  name: String!
  users: [User!]!
}